name = "arts-wire-glue"
main = "worker.js"
compatibility_date = "2024-11-01"

# Non-secret variable. Set your MailerLite group id here (or leave empty).
[vars]
MAILERLITE_GROUP_ID = ""

# Secrets are NOT stored here. Set them once from your terminal:
#   npx wrangler secret put LS_WEBHOOK_SECRET
#   npx wrangler secret put MAILERLITE_API_KEY
